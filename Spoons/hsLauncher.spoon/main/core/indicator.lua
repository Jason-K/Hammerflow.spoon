-- Legacy stub kept for compatibility while the original implementation lives in
-- main/backup/core. New call sites should require the backup module directly.
return require('hsLauncher.main.backup.core.indicator')
