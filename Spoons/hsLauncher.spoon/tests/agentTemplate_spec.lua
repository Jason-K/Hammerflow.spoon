-- Legacy agent template spec retired in 2025.
-- The agent system never shipped, so this file now short-circuits the test runner.

print("SKIP tests.agentTemplate_spec (retired agent template)")

return true